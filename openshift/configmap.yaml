apiVersion: v1
kind: ConfigMap
metadata:
  name: openshift-partner-labs-mcp-server-config
  namespace: ddis-asteroid--template
data:
  # MCP Server Configuration
  MCP_HOST: "0.0.0.0"
  MCP_PORT: "8443"
  MCP_TRANSPORT_PROTOCOL: "http"
  # Python Logging
  PYTHON_LOG_LEVEL: "INFO"
